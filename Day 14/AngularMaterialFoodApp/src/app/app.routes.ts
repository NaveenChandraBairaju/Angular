import { Routes } from '@angular/router';
import { FeedbackRatingComponent } from './feedback-rating/feedback-rating.component';

export const routes: Routes = [
  { path: '', component: FeedbackRatingComponent }
];